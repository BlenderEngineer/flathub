#!/bin/bash
# Runs on first install when the manifest uses type: extra-data for the Linux dist tarball.
# Sandbox only allows writes under /app/extra (cwd during install).
set -euo pipefail
cd /app/extra
tar --zstd -xf sibir-linux-amd64.tar.zst
chmod +x Sibir.x86_64 || true
